package com.enlear.erp.notification.exposed;

/** How urgent a notification is, driving UI emphasis and (later) delivery channels. */
public enum NotificationSeverity {
    INFO,
    WARNING,
    CRITICAL
}
